[Unit]
Description=Back up authentication and Chat databases from the active release
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/libexec/mediamtx-active-backup mediamtx-viewer
